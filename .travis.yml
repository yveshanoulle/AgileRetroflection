language: node_js

before_script:
  - npm install -g grunt-cli

node_js:
  - "0.12"
  - "4"

env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
